// Create a context object and then export it.  We only want to connect this component
//  to App, Field and Button components.
import React from 'react';

// No default value given here.
export default React.createContext();